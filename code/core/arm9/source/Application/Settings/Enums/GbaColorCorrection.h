#pragma once

/// @brief Enum representing the type of color correction to use.
enum class GbaColorCorrection
{
    /// @brief No color correction is applied.
    None,

    /// @brief Color correction is applied that resembles the AGB-001 screen.
    Agb001
};
